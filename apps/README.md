# Apps

`apps/` contains deployable application surfaces.

Current contents:

- `dashboard/backend/` for the FastAPI service
- `dashboard/frontend/` for the Dash UI

The legacy root paths `backend/` and `frontend/` are kept as compatibility symlinks during the transition so existing imports, scripts, and docs do not break.
