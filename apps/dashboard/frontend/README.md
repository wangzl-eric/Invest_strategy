# Frontend

`frontend/` is the Dash application.

Use this directory for:

- page layout and tab composition
- Dash callbacks and WebSocket UI integration
- styling and reusable UI components

Keep domain logic out of this layer. The frontend should consume APIs or shared library outputs rather than reimplement research or execution rules.
