[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
pythonpath = .
addopts =
    -v
    --strict-markers
    --tb=short
    --cov=dashboard.backend --cov=alpha_research.portfolio --cov=alpha_research.backtests --cov=alpha_research.execution
    --cov-fail-under=10
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow running tests
    requires_ibkr: Tests that require IBKR connection
    requires_db: Tests that require database connection
