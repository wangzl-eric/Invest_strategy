# Extensions

`extensions/` contains optional systems that are not required for the core dashboard or research workstation flows.

Current contents:

- `cerebro/` for research-ingestion and proposal-generation workflows

`qc_lean/` remains at the repository root for now because it includes a heavy local runtime and Lean submodule content. It should be treated as an optional external workspace.
