# Workstation

`workstation/` contains the quant research environment and shared quant libraries.

Current contents:

- `backtests/`
- `portfolio/`
- `execution/`
- `quant_data/`
- `research/`
- `notebooks/`
- `playground/`
- `books_and_papers/`

The legacy root paths are kept as compatibility symlinks for now so existing imports and scripts continue to work.
