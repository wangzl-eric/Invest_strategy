# Findings Log — Advances in Financial Machine Learning (López de Prado, 2018)

One line per session. Book studied chapter-by-chapter via `/read-to-learn`.

- **2026-06-17** — Read Ch. 7 (Cross-Validation in Finance). Central lesson: vanilla k-fold CV is structurally invalid in finance (non-IID: overlapping labels + serial correlation → leakage → false discoveries); fix is Purged K-Fold + embargo ($h \approx 0.01T$). **Partial validation (corrected same day):** `stats/cross_validation.py` implements `purged_kfold_split`/`cpcv_split`/`walk_forward_split` faithfully, and the ML-signal path (`signals.py` + `labeling.py`) feeds it a real `t1`. **But the weights-contract review pipeline does NOT call purged CV at all** — its "walk-forward" is `np.array_split(returns, 4)` (contiguous-segment Sharpe); look-ahead there is handled by the execution-convention shift + PIT macro instead. Defensible by design, not a bug. Open question from the note RESOLVED: review never invokes `purged_kfold_split`, so `label_end_times` is moot for it. Score 5/5/5. Note: `notes/ch07_notes.md`.
  - **Follow-up (same session) — acted on the finding:** upgraded the review's "walk-forward" from `np.array_split(returns, 4)` to genuine **expanding-window OOS** (40% in-sample anchor + 6 non-overlapping OOS windows) via a new backward-compatible `expanding=True` flag on `walk_forward_split`. Honestly an OOS *stability* diagnostic, not a leakage control. Logged in `research/STRATEGY_TRACKER.md` (2026-06-17); 20/20 targeted tests pass, flake8 clean.
